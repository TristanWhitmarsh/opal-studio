# =============================================================================
# requirements.txt — Stable GPU-enabled enviroment for Opal Studio
# Goal: Mesmer (DeepCell) + StarDist + Cellpose + InstanSeg with full GPU acceleration
#
# Setup Instructions:
#   1. conda create -n opal-all python=3.9
#   2. conda activate opal-all
#   3. conda install -c conda-forge cudatoolkit=11.2 cudnn=8.1 -y
#   4. pip install -r requirements.txt
# =============================================================================

# ── TensorFlow (Pinned for deepcell/Mesmer + StarDist) ──────────────────────
tensorflow==2.8.0
tensorflow-addons==0.16.1
tensorflow-io-gcs-filesystem==0.26.0

# ── PyTorch (Stable cu121 wheels to avoid DLL conflicts with TF 11.2) ───────
--index-url https://download.pytorch.org/whl/cu121
torch==2.1.2
torchvision==0.16.2
--extra-index-url https://pypi.org/simple

# ── NumPy (Pinned <2.0 for TF 2.8 and Torch 2.1 compatibility) ──────────────
numpy==1.26.4

# ── Segmentation Engines ───────────────────────────────────────────────────
deepcell==0.12.10
stardist==0.9.2
csbdeep==0.8.2
cellpose==3.1.1.3
instanseg-torch==0.1.1

# ── Scientific & Image Stack ────────────────────────────────────────────────
scipy==1.13.1
scikit-image==0.21.0
scikit-learn==1.6.1
matplotlib
imageio
tifffile
pillow
h5py
opencv-python-headless<5

# ── Numba / Performance ────────────────────────────────────────────────────
numba
llvmlite

# ── UI & Misc ───────────────────────────────────────────────────────────────
PySide6==6.10.3
tqdm
pydot
protobuf==3.20.3
packaging
six
